# Vercel server-side rewrite target for /api requests.
# Set this in Vercel Project Settings, e.g. https://readwick-api.onrender.com
API_ORIGIN=

# Optional fallback name if you already use this variable in Vercel.
VITE_API_URL=
